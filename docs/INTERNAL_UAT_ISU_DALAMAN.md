# [DALAMAN SAHAJA — JANGAN HANTAR KEPADA KLIEN]
## Log Isu Kritikal & Pembaikan — Sistem eShuttle (EKBK)

> Fail ini **bukan** sebahagian daripada dokumen UAT/FAT yang dihantar kepada EKBK. Ia dikeluarkan daripada `docs/UAT_FAT_PELAN_PENGUJIAN.md` atas permintaan (2 Ogos 2026) supaya dokumen klien kekal bersih — kandungan asal dikekalkan di sini untuk rujukan dalaman pembangun sahaja.

Semakan kod sumber dijalankan sebelum penyediaan pelan UAT/FAT untuk memastikan kes ujian yang dicadangkan menyasarkan risiko sebenar, bukan sekadar ciri "gembira" (happy path). Penemuan K1–K11 disahkan terus daripada kod. **Status semasa (31 Julai 2026 – 2 Ogos 2026)** — beberapa isu telah dibaiki; baki isu kekal seperti didokumenkan untuk rekod sendiri:

| # | Status | Penemuan | Fail/Rujukan | Kesan | Tindakan/Cadangan |
|---|---|---|---|---|---|
| K1 | 🟡 Ditangguhkan (BPM) | **Peranan BPM tidak disambungkan ke sebarang laluan.** Middleware `bpm` didaftar dalam `app/Http/Kernel.php` tetapi tiada `Route::middleware('bpm')` di `routes/web.php`. Mana-mana pengguna BPM yang log masuk akan mengalami `RouteNotFoundException` (ralat fatal 500). | `app/Http/Kernel.php:73`, `routes/web.php` | Pengguna BPM tidak boleh guna sistem selepas log masuk | Ditangguhkan — peranan BPM belum aktif digunakan buat masa ini |
| K2 | 🟡 Ditangguhkan | **Kelompok laluan pentadbiran (~74 laluan, baris 74–153 `routes/web.php`) tidak dilindungi oleh middleware `auth`** — termasuk `/admin/pengurusan-pengguna*`, `/admin/senarai-*`, `/admin/hak-milik-syarikat*`, `/admin/daerah*`, `/admin/status-permohonan-*`, `/admin/lampiran-permohonan*` dan versi `/bpm/*`. | `routes/web.php:74–153` | Berpotensi sesiapa (tanpa log masuk) boleh mencapai fungsi pentadbir, termasuk lampiran permohonan (gambar IC, sijil SSM) | Ditangguhkan buat masa ini bersama K1. Skop lebih luas daripada `/bpm/*` sahaja — turut merangkumi laluan teras `/admin/*` yang digunakan aktif oleh IPJPSM |
| K3 | ✅ Dibaiki | Tindakan "Hantar" oleh PHD guna `GET`, tiada semakan kelengkapan borang. **Dibaiki**: laluan `phd.batch.s{3,4,5}.hantar` ditukar kepada `POST` (kini dilindungi CSRF), dan `PhdController` kini menolak "Hantar" jika mana-mana Borang A–E belum disahkan | `app/Http/Controllers/Batch/PhdController.php`, `routes/web.php` | — | Dibaiki |
| K4 | ✅ Dibaiki | E-mel senyap gagal jika `MAIL_FROM_ADDRESS` tidak ditetapkan. **Dibaiki**: trait `GuardsMailFromAddress` merekodkan amaran log apabila saluran mel dilangkau | `app/Notifications/Concerns/GuardsMailFromAddress.php` | — | Dibaiki |
| K5 | ✅ Dibaiki | `CONTROL_PANEL_TOKEN` tiada dalam `.env.example`. **Dibaiki**: kini didokumenkan penuh | `.env.example` | — | Dibaiki |
| K6 | ⚪ Tidak diubah | Digit semakan (check-digit) nombor Kad Pengenalan Malaysia tidak disahkan dalam peraturan `MalaysianIC` | `app/Rules/MalaysianIC.php` | Nombor IC dengan digit terakhir salah masih diterima | Diputuskan tidak perlu dibaiki |
| K7 | ✅ **Dibaiki — kelemahan kritikal ditemui semasa pembaikan** | Dua sistem reset kata laluan berjalan selari, token reset = token CSRF. **Semakan lanjut mendedahkan**: `customChangePassword()` asal langsung tidak mengesahkan token terhadap e-mel sebelum menukar kata laluan — kelemahan **rampasan akaun (account takeover) tanpa had**, membolehkan sesiapa yang tahu e-mel seseorang menukar kata laluan akaun itu tanpa akses e-mel. Dibaiki: token kini rawak, di-hash, tamat tempoh 60 minit, disahkan penuh. Keputusan produk tambahan: admin kini boleh jana kata laluan baharu terus (butang "Jana Kata Laluan Baharu") | `ForgetPasswordController.php`, `PasswordReset.php`, `ResetPasswordMail.php`, `PengurusanPengguna/MainController.php`, `PasswordRegeneratedMail.php` | — | Dibaiki — ini adalah isu paling kritikal ditemui |
| K8 | ⚪ Tidak diubah (sengaja) | Pengesahan e-mel (email verification) adalah kod mati — aktivasi akaun 100% melalui kelulusan manual admin | `routes/web.php:37`, `User.php` | Bukan pepijat | Disahkan sengaja |
| K9 | ⚪ Tidak diubah | Log audit direkod tetapi tiada UI untuk melihatnya | `AdminController.php` | Ciri tidak boleh diakses | Diputuskan tidak perlu buat masa ini |
| K10 | ✅ Dibaiki | `wkhtmltopdf` (snappy) sebagai enjin PDF lalai — bergantung binari luaran. **Dibaiki**: ditukar kepada `dompdf` (pustaka PHP tulen, tiada pergantungan binari) | `config/report-generator.php` | — | Dibaiki |
| K11 | ⚪ Tidak dianggap isu | `APP_DEBUG=true` sebagai lalai dalam `.env.example` | `.env.example:4` | — | Diputuskan bukan risiko — sahkan `false` di pengeluaran sebagai amalan baik standard |

**Nota**: senarai ini sepadan dengan versi 1.0 dokumen UAT/FAT (31 Julai 2026) sebelum Bahagian "Isu Kritikal" dikeluarkan daripada versi klien pada 2 Ogos 2026. Rujuk `docs/UAT_FAT_PELAN_PENGUJIAN.md` untuk dokumen semasa yang dihantar kepada EKBK.
